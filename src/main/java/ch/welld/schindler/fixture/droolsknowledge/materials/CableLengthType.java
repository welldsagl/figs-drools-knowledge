package ch.welld.schindler.fixture.droolsknowledge.materials;

/**
 * Required cables can have a `STANDARD` length or an 'ON COMMISSION' one. This enum represents this
 * two possibilities.
 */
public enum CableLengthType {
    STANDARD,

    ON_COMMISSION
}
